using UnityEngine;

namespace Environment.Moon
{
    public class MoonPlayerTracker : MonoBehaviour
    {
        // Looks at the closest player and tracks them. 
        // If no players are in range then it'll go back to default pos.
    }
}